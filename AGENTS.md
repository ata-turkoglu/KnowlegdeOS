# Agent Entry Point

Canonical AI documentation: [`AI_CONTEXT.md`](AI_CONTEXT.md).

Read it first, then follow the nearest subtree `AI_CONTEXT.md`. Preserve the project rules in `PROJECT.md` and `docs/CODEX-RULES.md`.

